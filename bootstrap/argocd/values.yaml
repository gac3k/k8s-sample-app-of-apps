applicationSet:
  enabled: true

configs:
  # The source-of-truth repo is public, so registering it is purely
  # informational -- Argo CD can clone it anonymously over HTTPS.
  repositories:
    k8s-sample-app-of-apps:
      url: https://github.com/gac3k/k8s-sample-app-of-apps.git
      type: git
